<p class="back"><%= link_to "« Back to list", links_path %></p>
<div class="box">
  <div class="data">
    <h4>Short name</h4>
    <p><%= @link.name %></p>
  </div>
  <div class="data">
    <h4>Website url</h4>
    <p><%= @link.url %></p>
  </div>
  <div class="data">
    <h4>Your link</h4>
    <p><%= link_to redirect_link_url(@link.name), redirect_link_url(@link.name) %></p>
  </div>
  <div class="options">
    <%= link_to "Edit", edit_link_path(@link), class: 'btn btn-ui' %>
    <%= link_to "Delete", link_path(@link), method: :delete, class: 'btn btn-ui' %>
  </div>
  <div class="callout">
    <%= render "links/message" %>
  </div>
</div>
